Resolve catalog-control request `CAT-7900` using the active `PRICE_POLICY` record and every trusted control record it references in Stripe. Normalize the catalog only when that evidence, an open change window, and a unique live price agree; otherwise make no changes. Return a concise JSON decision report with the request, decision, matched price facts, and evidence.
