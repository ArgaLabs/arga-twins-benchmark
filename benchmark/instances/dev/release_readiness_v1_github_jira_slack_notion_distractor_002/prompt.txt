Assess release `REL-205` using Notion page `[REL-205] Release checklist`. It is READY only if every item under `Required changes` is merged, every item under `Blocking items` is Done or closed (or the list says `none`), and `Security approval` is `APPROVED`. Check failures in that order. Add exactly one comment to the tracker item marked `REL-205 release record` and post exactly one message to `#releases` using `READINESS REL-205: <decision> - <reason>.` Use `all gates passed`, `change <ref> is not merged`, `blocker <id> is not done`, or `security approval missing` as the reason. If READY, move the release record to Done or completed; if BLOCKED, preserve its current state. Do not edit code, the checklist, other tracker items, or other channels. Return exactly the posted line.
