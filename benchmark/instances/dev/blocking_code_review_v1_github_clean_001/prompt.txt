Find the open code change in repository `acme/web-parser` linked to `SEC-204`. If it evaluates an HTTP request value as executable code, submit one `REQUEST_CHANGES` review with body exactly `Blocking: untrusted input reaches eval.` and attach one inline comment to the vulnerable line with body exactly `Replace eval with a non-executing parser.` Do not modify branches, push, merge, close, approve, or comment on any other change. Return `REVIEWED <change-number>`.
