Audit approved specification `[SPEC-90] Retry policy` against repository `acme/api-gateway` and Jira project `SEC`. Apply the specification-audit policy in the knowledge base, evaluate every required behavior at its referenced implementation, and reconcile the result with existing exact-marker tracker work. Preserve the specification, repository, and existing tracker records; create no duplicate work. Return a compact JSON fact report with the specification, evaluated and missing requirement IDs, tracker action, and tracker identifier.
