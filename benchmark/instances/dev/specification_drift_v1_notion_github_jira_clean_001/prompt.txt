Audit approved specification `[SPEC-88] HTTP logging` against repository `acme/api-gateway` and Jira project `SEC`. Apply the specification-audit policy in the knowledge base, evaluate every required behavior at its referenced implementation, reconcile the result with existing tracker work, and create only genuinely missing work. Preserve the specification, repository, and existing tracker records. Return a compact JSON fact report with the specification, evaluated and missing requirement IDs, tracker action, and tracker identifier.
