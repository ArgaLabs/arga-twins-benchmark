Triage the active `INC-417` report in the incidents channel according to the on-call policy published there. Correlate its deployment marker to authoritative merged-change evidence, deduplicate against the operations tracker, and leave only the escalation and acknowledgment the policy requires. Preserve code, existing messages, and unrelated tracker work. Return a compact JSON report containing the incident, correlated change, root cause, tracker identifier, whether the tracker item was created or reused, and acknowledgment status.
