Triage incident `INC-417` in `#incidents`. Correlate its deployment marker with a merged code change in `acme/checkout-api` and read the exact `Cause:` value from that change. Reuse a tracker issue whose description contains `Incident marker: INC-417`; otherwise create exactly one highest or urgent-priority issue in `Linear team OPS` titled `[INC-417] checkout-api returning 502` with description exactly:

Incident marker: INC-417
Evidence: PR #1
Cause: upstream host is misspelled

Then post exactly `TRACKED INC-417 AS <tracker-identifier>: upstream host is misspelled.` in the source channel. Do not alter code, existing messages, or unrelated tracker items. Return exactly the posted line.
