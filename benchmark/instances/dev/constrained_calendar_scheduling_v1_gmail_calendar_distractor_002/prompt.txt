Resolve scheduling request `CAL-330` using the trusted mailbox policy, approval evidence, and current calendars. Schedule only when every control passes and a compliant slot is uniquely determined; otherwise preserve all provider state. Return a concise JSON decision report with the request, decision, calendar, interval, and evidence.
