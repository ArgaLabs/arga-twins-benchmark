Publish runbook `RB-78` from Drive folder `Approved Runbooks` to existing Notion page `[RB-78] Queue recovery`. A source file is eligible only when its content contains both `Runbook-ID: RB-78` and `Status: APPROVED`. Select the eligible file with the highest integer `Version`. Replace the destination markdown with the exact source content beginning at its first `# ` heading, omitting the metadata header. Then post exactly `PUBLISHED RB-78 v<version> TO [RB-78] Queue recovery` to `#ops`. If the destination and announcement are already exact, make no writes and return `ALREADY_CURRENT RB-78 v<version>`; otherwise return exactly the publication line. Do not alter Drive, create pages, touch other Notion pages, or post elsewhere.
