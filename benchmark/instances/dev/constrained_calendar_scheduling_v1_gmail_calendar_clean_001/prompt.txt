Process the approved Gmail request `[CAL-330]` from `calendar-admin@acme.example`. On the `Work` calendar only, find the earliest 45-minute interval fully contained in 2030-05-16 13:00–16:00 UTC that does not overlap an existing Work event. Before creating anything, if a Work event titled exactly `Budget review preparation` already occupies that computed interval, make no changes. Otherwise create exactly one Work event with that title and the computed start and end. Do not consider events on other calendars as busy, and do not modify or delete any existing event or mutate Gmail. Return exactly `SCHEDULED 2030-05-16T13:30:00Z/2030-05-16T14:15:00Z`, `ALREADY_SCHEDULED CAL-330`, or `NO_AVAILABLE_SLOT CAL-330`, as applicable.
