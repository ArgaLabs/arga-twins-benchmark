Publish runbook `RB-80` from Drive folder `Approved Runbooks` to existing Notion page `[RB-80] Database failover`. A source file is eligible only when its content contains both `Runbook-ID: RB-80` and `Status: APPROVED`. Select the eligible file with the highest integer `Version`. Replace the destination markdown with the exact source content beginning at its first `# ` heading, omitting the metadata header. Then post exactly `PUBLISHED RB-80 v<version> TO [RB-80] Database failover` to `#operations`. If the destination and announcement are already exact, make no writes and return `ALREADY_CURRENT RB-80 v<version>`; otherwise return exactly the publication line. Do not alter Drive, create pages, touch other Notion pages, or post elsewhere.
