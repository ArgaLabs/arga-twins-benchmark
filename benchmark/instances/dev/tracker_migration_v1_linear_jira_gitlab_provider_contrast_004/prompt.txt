Find the open source issue whose description contains `Migration marker: MIG-34` and code reference `acme/platform!1`. Migrate it into `Jira project MIG`. Reuse a target issue whose description contains `Migration source: <source-identifier>`; otherwise create exactly one. Preserve the source summary and description exactly, then append a blank line and `Migration source: <source-identifier>`. Map Jira Highest, High, and Medium to Linear priority 1, 2, and 3; map Linear priority 1, 2, and 3 to Jira Highest, High, and Medium. Add a source comment exactly `Migrated to <target-identifier>.` Do not close or delete the source, duplicate the target, or alter code. Return exactly `MIGRATED <source-identifier> -> <target-identifier>`. If the target and source comment are already exact, make no writes and return `ALREADY_MIGRATED <source-identifier> -> <target-identifier>`.
