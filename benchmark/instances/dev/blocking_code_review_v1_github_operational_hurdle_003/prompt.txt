Find the open code change in repository `acme/web-parser` linked to `SEC-205`. If its diff evaluates an HTTP request value as executable code, submit exactly one `REQUEST_CHANGES` review with body `Blocking: untrusted input reaches eval.` and exactly one inline comment on the vulnerable line with body `Replace eval with a non-executing parser.` Do not modify branches, refs, repository contents, labels, assignees, approval state, merge state, or any other change. Return exactly `REVIEWED #31`.
