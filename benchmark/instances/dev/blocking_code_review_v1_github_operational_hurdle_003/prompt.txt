Act as the security reviewer for the currently queued expression-parser work in `acme/web-parser`. Follow the repository's checked-in security-review policy, locate the relevant change in the large open queue, validate it against changed-file evidence, and create only the warranted blocking review artifacts. Preserve code, refs, pull-request lifecycle and metadata, and every unrelated review. Return a compact JSON report identifying the decision, target change, and artifacts created.
