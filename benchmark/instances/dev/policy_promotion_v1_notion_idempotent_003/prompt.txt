Resolve governance ticket `POLICY-27` using the active policy standard and structured approval records in the connected workspace. Apply only the lifecycle transition the evidence authorizes, remain idempotent when it is already complete, and return a concise JSON decision report with the ticket, decision, predecessor, successor, and evidence.
