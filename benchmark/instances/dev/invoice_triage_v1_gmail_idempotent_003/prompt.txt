Process the current AP review batch under policy AP-17 using the authoritative policy, vendor roster, and batch manifest in the mailbox. Verify existing finance-review artifacts before writing, leave messages unsent, and do not duplicate correct work. Return a concise JSON report with the policy version and classify every batch invoice as prepared, already prepared, or ineligible, with a short reason for each ineligible item.
