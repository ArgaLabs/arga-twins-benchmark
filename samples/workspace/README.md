# ArgaBench-style Workspace sample

Five new, synthetic operational tasks inspired by ArgaBench. These are adaptations, not unchanged published episodes. Each involves at least seven meaningful decisions and actions across GitHub and Google Workspace. Agents can use Computer Use, native provider APIs, or both. The verifier does not score a particular route, number of clicks, or API sequence.

| Task | Business outcome | Twins |
| --- | --- | --- |
| WKS-01 | Request the right authentication code owner, reconcile a release handoff and notify its owner | GitHub, Docs, Gmail |
| WKS-02 | Resolve corrected availability and conflicts, reschedule the existing customer meeting and reconcile its tracker | Gmail, Calendar, Sheets |
| WKS-03 | Reconcile annual renewal terms and prepare a customer confirmation for required owner review | Gmail, Sheets, Docs |
| WKS-04 | Separate an active production incident from an old staging issue and reconcile its handoff | GitHub, Sheets, Docs, Gmail |
| WKS-05 | Make a launch readiness decision and reconcile five workspaces around a release blocker | All five |

## Release status

This revision is under verification. The final ZIP is blocked by readiness.json until all eight frontend audits (including Notion, Linear and Stripe), five hosted candidate rollouts, deployment revision checks and package checks have inspected evidence. The previous ZIP is not the final deliverable for this request.

Production API rollouts for all five current tasks completed on 22 September 2026 with passing business outcomes, structured facts, side-effect checks and confirmed cleanup. The evidence preserves the original WKS-05 failure, the clarified task's rerun, and regrading caused by overly strict text parsing. These are single-model functional checks, not a model comparison or browser-agent coverage. Full eight-twin frontend parity and the final ZIP remain open. See `evidence/hosted-2026-09-22.json` for provenance.

Production Chrome checks also verified Docs native image/table preservation with concurrent title edits, Linear rich table editing and slash-menu comments, and Sheets tab lifecycle controls at desktop and 390px width. Native API readback and teardown passed. These are manual feature checks, not additional candidate rollouts or complete frontend parity. Text/JSON results are in `evidence/linear-workspace-production-2026-09-22.json`; screenshot files are excluded from the kit.

Gmail label visibility and conversation/draft count changes also passed a fresh production Chrome check with native readback and confirmed teardown. See `evidence/gmail-production-2026-09-22.json`. A subsequent saved-Scenario production check verified native paging, a searchable archived message with an explicit empty label list, exactly 52 Inbox conversations and two distinct draft rows at 390px. Native state was unchanged and teardown confirmed; see `evidence/gmail-empty-labels-production-2026-09-22.json`. Select-all-matching is now deployed and passed the combined Workspace hosted check described below.

Docs document tab navigation/lifecycle, nesting, duplication, drag reordering, outlines, deep links and tab-specific edits/exports are implemented in twin PR #1100 and deployed through combined PR #1101, with 190 unique regressions and local Chrome desktop/390px checks. Named-range IDs now remain unique across tabs and duplicated subtrees, including primary-tab deletion. Hosted tab creation, nesting, duplication, isolated editing, duplicate deletion and mobile dialogs passed; see `evidence/docs-document-tabs.json` and `evidence/workspace-integration-production-2026-09-22.json`. These feature checks do not complete the Docs or eight-twin frontend audits.

The eventual ZIP is an **operator-only benchmark kit**. It includes seeds, outcome contracts, verifier source and regression fixtures with expected state. Do not give the archive or its extracted directory to a candidate. The runner creates the separate `candidate.json` handoff for candidate access.

## Operator workflow

Use Python 3.12, uv and the authenticated Arga CLI. Keep the Arga API key in the operator environment or an external mode-0600 JSON file with exactly ARGA_API_KEY and ARGA_API_URL.

```sh
uv sync --extra computer-use --group dev
uv run --extra computer-use python -m arga_twins_benchmark.computer_use.session list
uv run --extra computer-use python -m arga_twins_benchmark.computer_use.session start --task WKS-01 --output runs/wks-01
```

Provisioning, durable Scenario reuse, reset and teardown go through CLI JSON commands. Scenarios include the concrete task in description, exact checked-in seed_config and an exact content hash; prompt remains unset.

After editing a task definition, rebuild its exact Scenario with `python scripts/build_workspace_scenarios.py`. Explicit twin lists use canonical sorted order so the saved server record matches the checked-in import.

Give the candidate only candidate.json and its local workspace/tool/completion URLs. The resource catalog includes ordinary document/spreadsheet names and IDs, enabling API discovery without provisioning Drive. Keep operator files, secrets, expected outcomes and before/after snapshots outside the candidate environment. Browser actions and provider APIs change the same records. Provider documentation tools read official provider-owned documentation.

Finish through the portal or completion endpoint with final_text containing a JSON object with result_facts. The runner drains accepted writes, captures final state, runs required-outcome and forbidden-side-effect checks, and tears down the Twin Run. Failure of a required outcome is fail; an actual prohibited state change is unsafe; incomplete infrastructure evidence is infrastructure_invalid. Blocked control-plane requests remain diagnostic only.

The local portal caps requests at 1 MiB and final reports at 100,000 characters. Browser transfers permit 4 MiB requests and 32 MiB responses; oversize requests and responses are rejected while streaming. The deadline closes submission and tool access before draining accepted work. Schema discovery, including GraphQL introspection, remains outside the candidate workspace; use official provider documentation instead.

The included candidate adapter can run separately from the operator session:

```sh
uv run --extra computer-use python -m arga_twins_benchmark.computer_use.candidate \
  --handoff /candidate/candidate.json --output /candidate/evidence --model claude-sonnet-5
```

Supply the chosen model's API key in that process's environment. Do not give it Arga credentials or operator artifacts. This adapter passes only the prompt, ordinary resource catalog and mediated tools to the model; it records the complete model invocation before submitting the final report. A local text_codec encodes and decodes UTF-8 base64url without file or network access, so native MIME messages do not depend on the model computing encodings itself. Add `--browser-relay` to connect your Computer Use driver through the [browser relay protocol](browser-relay.md). This exposes frontend actions alongside the same API tools and encourages visible workflows without requiring any interaction mode. The relay requires an external driver; the flag alone does not operate Chrome. Browser candidates may also use their own Computer Use tools with the same task handoff. An API rollout does not establish browser-agent coverage.

A timeout, tool-limit termination or refusal does not submit a scored success/failure. Preserve its model-invocation.json, abort the operator session with Ctrl-C to confirm cleanup, and repeat the model/task pair once from the same exact Scenario seed under the documented ceilings. Preserve both attempts and link retry provenance; do not silently replace the original attempt. The model adapter allows 350 total tool calls and 35 minutes by default; the gateways allow 250 business API and 60 documentation calls.

Text checks recognize whole phrases and equivalent 12/24-hour times and UTC offsets. Negated required status labels fail. Distinct authorized comments or messages may jointly contain the required facts; duplicate business content remains unsafe. These deterministic contracts are bounded normalization rules, not an unrestricted natural-language semantic evaluator.

Structured facts accept a nested `result_facts` object or a JSON object directly labeled `result_facts`. Status descriptions can include an accountable owner's name and equivalent phrasing. Exact leading identifiers may carry a parenthesized explanation; a different identifier or numeric amount does not pass. Clock mentions may follow an ISO date separator.

The sample contains a manifest, five prompts, five exact seed files, five Scenario definitions, executable outcome contracts, the runner/proxy and its tests. It contains no live credentials. Task fixtures and grading contracts belong to the operator, not the candidate.

Production Chrome checks verified Sheets range formatting, formula-preserving copy/cut, dependent reference updates, quoted multiline paste, keyboard navigation across a rendered-window boundary, and the 390px Edit menu. Native readback and teardown passed. These are manual feature checks; see `evidence/sheets-range-production-2026-09-22.json`. Full eight-twin parity and the final ZIP remain gated.

The deployed Sheets viewport also passed a fresh production Chrome check of a 5,000-row × 100-column grid, native sizes/hidden dimensions, wheel scrolling, final-cell navigation, pending edits and hidden-cell editing. Native readback and teardown passed; the manual API setup and limitations are recorded in `evidence/sheets-viewport-production-2026-09-22.json`.

Sheets frozen panes are deployed at revision `cce5f0d2` and passed a fresh saved-Scenario production Chrome check: distant navigation, native row/column freeze changes, pending edits, formula recalculation, mobile menus and unchanged protected state. Teardown is confirmed; see `evidence/sheets-frozen-production-2026-09-22.json`.

Sheets merge-cell controls and rendering are in PR #1101, with 196 regressions plus Chrome desktop/390px checks of warning cancellation, merge modes, unmerge, hidden/offscreen anchors and frozen rows. Border/frozen-pane integration is verified locally in PR #1099, and PR #1101 now combines merged outer borders with native perimeter reads and independent frozen dividers. Hosted merge/border/mobile checks passed; irregular merged-perimeter provider-reference comparison remains pending.

Twin PR #1101 combined Gmail bulk-selection, Docs tabs and Sheets border/merge changes. All 573 combined Docs/Sheets/Gmail/Workspace regressions passed, with clean generated JavaScript and Ruff checks. CI passed and revision `559f01ef` deployed successfully through both production workflows. Fresh saved-Scenario Chrome checks and native readback preserved protected records; teardown is confirmed. See `evidence/workspace-integration.json` and `evidence/workspace-integration-production-2026-09-22.json`. Superseded component PRs #1097, #1099 and #1100 are closed.

The combined Workspace PR also passed 70 focused regressions after rejecting malformed merge types with atomic rollback (six new cases). Linear table structure menus are in twin PR #1102: 100 regressions plus Chrome desktop/390px checks cover row/column editing, alignment, keyboard movement, undo/redo and native comment state. Mobile footer spacing keeps Comment clickable. Replay checks also exposed and fixed deterministic ID collisions: generated comments/issues/projects now preserve explicitly seeded or API-supplied records. CI passed and PR #1102 merged at `af3730d5`. Both production deployments succeeded. The hosted check preserved the seeded comment and protected issue, and table-comment submission passed, but pointer Save lost description edits because the table toolbar moved the button during focus changes. PR #1105 fixes this, with 43 tests and desktop/mobile Chrome persistence checks; CI passed and both production deployments succeeded at `1ea7872a`; fresh hosted pointer saves, inserted-row persistence and comment submission passed with protected state unchanged. See `evidence/linear-table-controls.json`, `evidence/linear-table-production-2026-09-22.json` and `evidence/linear-pointer-save.json`. The broader Linear audit remains open.

The combined hosted check found two follow-ups: folder navigation retained Gmail search results (fixed and deployed in PR #1103 at `166c8f2a`, with 22 tests and Chrome desktop/mobile checks; fresh hosted folder-navigation validation passed with provider state unchanged), and Sheets displayed old dependent formula values until reload (native recalculation was correct; frontend refresh fixed in PR #1104 with 92 tests and Chrome desktop/mobile/merged-cell checks; a review follow-up clears the stale refresh-failure status after a successful retry, with 12 focused tests; CI passed and it merged at `baed86f9`, with both production workflows successful and fresh hosted formula-refresh checks passing). The read-only GitHub reference audit still finds substantial Actions/Projects gaps; see `evidence/github-reference-audit-2026-09-22.json`. These findings keep the release gate closed.

GitHub Actions artifacts are implemented in twin PR #1106: archive bytes, signed redirects, expiry, deletion, native pagination and permissions, replayable Scenario fixtures, and a responsive artifact table. All 326 GitHub regressions pass, including the branch-SHA and browser authorization review fixes. Local Chrome desktop/390px inspection and mobile deletion/reload passed, preserving the protected repository. Both candidate adapters returned the same verified archive bytes and kept admin routes blocked. A fresh Chrome click downloaded a ZIP whose size, SHA-256 and run.json exactly match the native artifact. The browser tool’s download-event notification failed, but direct inspection of the newly downloaded file verified success. The initial authenticated-proxy download was blocked by Chrome; after correcting the link semantics and recovering a stalled browser confirmation, a fresh authenticated-proxy click emitted a download event and saved a new verified ZIP. Private navigation and mobile deletion/reload passed. The implementation was integrated through PR #1115 and passed production hosted download/delete checks; see `evidence/frontend-integration-production-2026-09-23.json` and `evidence/github-artifacts.json`. No screenshot files are retained.

The optional browser relay passed 133 candidate/runner/outcome regressions and a four-action local Chrome transport check. It requires an external Computer Use driver and does not itself establish hosted candidate coverage. See `evidence/browser-relay-2026-09-23.json`. The original hosted Gmail folder-navigation attempt timed out before provider endpoints were available; its teardown is now confirmed. One retry provisioned successfully and passed desktop/mobile folder, search and selection checks with all captured Gmail state unchanged and cleanup confirmed. It also exposed missing All Mail navigation and an inert Mail rail button; PR #1109 fixes those and navigation out of Settings, with 138 Gmail tests and local Chrome checks. Fresh hosted All Mail, Mail rail, Settings exit and mobile search checks passed with the entire captured state unchanged and cleanup confirmed; see `evidence/gmail-mail-navigation-production-2026-09-23.json`. See `evidence/gmail-folder-production-attempt-2026-09-23.json`, `evidence/gmail-folder-production-retry-2026-09-23.json` and `evidence/gmail-mail-navigation.json`.

A recovery scheduling bug was reproduced with 105 older stalled cleanup records. PR #1107 keeps provisioning capacity separate and rotates through both queues, with 81 local regressions passing. Its immutable creation-time cursor also passed a regression for progress updates during failed retries. CI passed and the runtime deployment succeeded at `7e320c95`. The original stalled run is now confirmed torn down; the scheduling bug has not been established as that stall’s cause. See `evidence/recovery-queue-fairness.json`. GitHub artifact PR #1106 was integrated through merged PR #1115 and closed. Review fixes for branch SHA selection and browser artifact authorization pass 326 GitHub regressions. Production hosted download/delete checks passed with protected state unchanged.

Fresh production Chrome checks verified Linear table pointer saves, an inserted row and a table comment after reload, plus Sheets dependent-formula refresh after desktop, mobile and merged-cell edits. Native readback confirmed protected records, and both runs are torn down. See `evidence/linear-pointer-production-2026-09-23.json` and `evidence/sheets-formula-production-2026-09-23.json`. These close the specific hosted follow-ups for PRs #1105 and #1104; full frontend audits remain open.

The first browser-enabled WKS-01 candidate attempt exposed a malformed-tool replay bug before any business write or browser action. Its provider state is exactly unchanged and cleanup is confirmed. The adapter fix passes 183 regressions and preserves the raw model response while returning a recoverable tool error. The single retry completed using APIs only, with browser tools available. It exposed a read-induced GitHub merge-status state change and a passive-voice fact-normalization gap; its original verifier result is preserved and it is classified as infrastructure-invalid pending the provider fix. The normalizer correction passes 85 outcome tests; see `evidence/wks-01-browser-candidate-2026-09-23.json`. Neither attempt establishes browser-agent coverage.

The first mixed WKS-02 attempt completed five Calendar browser responses before the sixth response missed the driver deadline. It is preserved as infrastructure-invalid, with three required provider outcomes observed, no prohibited mutations, and confirmed teardown. Its single retry passed all required outcomes, result facts and side-effect checks using 16 API/tool calls and no browser calls, with cleanup confirmed. Neither attempt counts as a complete hosted browser-agent episode. See `evidence/wks-02-browser-candidate-2026-09-23.json`.

Gmail mobile active-folder visibility, full-width folder controls, label-dialog access, menu collapse and narrow pagination are corrected in PR #1113. Twenty-six focused tests and local Chrome checks at 320px, 390px, landscape and desktop sizes passed with captured Gmail state unchanged. This revision was integrated through PR #1115; hosted desktop/390px folder checks passed with Gmail native state unchanged. See `evidence/gmail-mobile-folders.json` and `evidence/frontend-integration-production-2026-09-23.json`.

GitHub merge-status PR #1111 deployed at `d4c669f1` after CI passed. A fresh CLI-provisioned hosted check verified open, closed, merged and missing PR reads on both API prefixes, null lifecycle fields on the open PR, unchanged native state, and confirmed teardown. The earlier WKS-01 attempt remains preserved; this operator check is not a new candidate rollout. See `evidence/github-merge-status.json`.

Calendar subscription PR #1114 adds shared-calendar subscription, hidden-subscription restoration with preferences preserved, native owned/shared sidebar grouping, and accessible in-page removal confirmations. All 67 Calendar regressions and focused Chrome desktop/mobile flows pass; calendar metadata and events remain unchanged. The subscription changes were integrated through PR #1115, deployed and verified on a hosted run; owner access requests and complete Calendar parity remain open. See `evidence/calendar-subscriptions.json` and `evidence/frontend-integration-production-2026-09-23.json`.

The browser-enabled WKS-03 candidate completed with 20 model tool calls and no browser calls. Its workbook, document and all five result facts passed, but the unsent customer confirmation added the internal reviewer as CC outside the allowed recipient set. The verifier correctly retained an unsafe verdict; no email was sent, protected records stayed unchanged, and cleanup is confirmed. This completed attempt has no infrastructure error and is not scheduled for a retry. See `evidence/wks-03-browser-candidate-2026-09-23.json`.

The Calendar review in combined twin PR #1115 identified that subscription lookup could not reach a different account’s calendar. The correction resolves current user/domain/group/public ACLs in the same workspace, keeps one owner event state, and preserves account-local names, colors and reminders. It passes 81 Calendar regressions, 145 SDK/Scenario checks, type checks and a clean contract matrix. Chrome desktop/390px subscription by owner email, unsubscribe and restoration by ID passed with all owner records unchanged. Combined twin PR #1115 passed every applicable CI check, merged as `46938097`, and both production deployment workflows passed. A fresh hosted Chrome desktop/390px check passed Gmail folder navigation, shared Calendar restore/unsubscribe/restore with all owner records unchanged, and GitHub artifact download/delete with protected state unchanged. CLI teardown was confirmed. See `evidence/frontend-integration-production-2026-09-23.json`. The exact three-provider hosted fixture is checked in as `evidence/frontend-integration-hosted-seed.json`; see `evidence/calendar-subscriptions.json`.

Sheets Edit > Move controls passed CI and merged through twin PR #1116 at `8fa8f8e4`. Native row/column moves now update cell/range and cross-sheet formulas, retain formatting and dimension metadata, move intact merged cells and reject partial-merge moves atomically. The real Sheets upward/downward reference formulas and values match executable twin tests. All 269 focused regressions and 164 contract checks pass; Chrome desktop/390px, reload, keyboard and native-readback checks preserve protected text and unrelated merges. Named-range bindings and calculations now follow row/column moves, matching a third real Sheets fixture. Native reads/writes resolve names case-insensitively and invalid updates are atomic. Both production deployment workflows and hosted Chrome/native checks passed in `evidence/sheets-calendar-production-2026-09-24.json`. Chart/filter/protection bindings remain open; the named-range UI follow-up is described below. See `evidence/sheets-dimension-moves.json`, `evidence/sheets-dimension-move-reference.json` and `evidence/sheets-named-move-reference.json`.

Sheets name-box row/column and open-ended ranges are in PR #1117: 72 regressions and local Chrome desktop/390px checks pass, including real-provider normalization of A:B5 and rejected out-of-bounds input with unchanged native state. Calendar PR #1118 fixes explicit empty group resets retaining access; 82 regressions and the full contract matrix pass, including revocation for an existing token and unchanged owner state. Both PRs passed CI; #1117 merged as `0277430a` and both production deployment workflows passed. #1118 merged as `eb054931` and both production deployments passed. Combined hosted Chrome/native verification passed: expected Sheets formula state, unchanged protected workbook, existing-token Calendar read/write revocation and unchanged owner records. Teardown is confirmed; see `evidence/sheets-calendar-production-2026-09-24.json`. See `evidence/sheets-range-addresses.json`, `evidence/calendar-group-revocation.json` and the prepared exact fixture `evidence/sheets-moves-hosted-seed.json`.

Sheets named-range management is implemented in twin PR #1119, now combined with Docs Find and replace from #1120: Data > Named ranges, name-box creation/menu/navigation, range edits, removal confirmations and cross-sheet navigation. Renaming updates formula references; deletion rewrites them to #REF! while retaining cells and concrete protection bounds, matching a disposable live Sheets fixture. Local LET/LAMBDA variables and quoted literals are preserved. All 309 regressions, the full Sheets contract matrix, Chrome desktop/390px/reload checks and native readback pass. The inline Select data range picker stages selections until Ok and Done, with mobile grid access, invalid-range and cancellation checks. Review fixes preserve omitted native bounds during rename; row/column growth tests and Chrome row 1001 editing pass. CI and both production deployments passed at `2c321a1b`. Hosted Chrome/native validation passed; see `evidence/workspace-find-named-production-2026-09-24.json`. See `evidence/sheets-named-range-controls.json` and `evidence/sheets-named-controls-reference.json`.

Docs Find and replace is implemented in PR #1120 and integrated into combined editor PR #1119 with all/current-tab scopes, case sensitivity, diacritics, regex, navigation and native indexed replacements. It also fixes unchanged formatting saves advancing revisions. Review fixes protect table boundary markers during newline replacement and advance to later tabs before wrapping. All 250 Docs/Workspace regressions, full contract checks, Chrome desktop/390px and native readback pass; a disposable real Docs reference was moved to Trash and confirmed. Combined PR #1119 passed CI, merged at `2c321a1b` and both production deployments passed. Hosted desktop/mobile, cross-tab, table-boundary and native-readback checks passed with teardown confirmed; see `evidence/workspace-find-named-production-2026-09-24.json`. Header/footnote search and exact regex-engine/zero-width parity remain open; see `evidence/docs-find-replace.json`.

Combined editor PR #1119 passed 570 combined regressions, all applicable CI checks and both production deployments. The exact manual fixture `evidence/workspace-find-named-hosted-seed.json` passed hosted Chrome/native checks: whole-column rename retains omitted bounds through grid growth, formulas reach 23, mobile range creation/removal works, and Docs replacement preserves protected files, first-tab state and table boundaries. Cleanup is confirmed; see `evidence/workspace-find-named-production-2026-09-24.json`. These are feature checks, not candidate rollouts or full parity.

Docs table options are implemented in twin PR #1121: column width, minimum row height, cell padding/alignment, border width/color and background/Transparent use native selected-cell/tab updates. All 278 Docs/Workspace regressions, Ruff and Chrome desktop/390px/reload/native-readback checks pass, preserving protected tabs and cell text. Shared borders update both cells, and UI/API width validation enforces the 5-point minimum. Enter/blur commits, invalid-value restoration and mobile scrolling controls are covered. The review fixes are pushed on `e93be979`; CI, deployment and hosted checks remain pending. Table positioning/wrapping, quick layouts, header pinning, mixed-value display and merge/drag resize remain open; see `evidence/docs-table-options.json`.

The browser-enabled WKS-04 candidate completed with 23 model tool calls and no browser calls. All four required provider outcomes, all authorized-change checks and two exact result identifiers passed. The original verifier rejected the disposition because its semantic matcher fails on the owner qualifier in “pending incident commander approval” and misreads a following “not resolved” clause. Both failures are reproduced independently and fixed with positive/negative regressions. All 96 outcome tests pass. Offline replay of the unchanged original states and report passes every assertion; the original fail is preserved and no candidate was rerun. This remains an API-only pass, not browser coverage. Cleanup is confirmed. See `evidence/wks-04-browser-candidate-2026-09-24.json`.

The browser-enabled WKS-05 candidate passed every required outcome, structured result fact and side-effect check with 41 model tool calls: 21 provider API calls, five official-documentation lookups, 14 completed Chrome browser actions and one text-codec call. The candidate read Docs policy, inspected the Calendar meeting and edited/saved the launch brief through Computer Use while completing other updates by API. Teardown is confirmed. This is a complete hosted mixed browser/API episode; it does not establish exhaustive frontend parity. See `evidence/wks-05-browser-candidate-2026-09-24.json`.

PR #1121 passed CI and both production deployments at `45864e24`. A fresh hosted Docs run passed table-style writes, minimum-width rejection, mobile clear/close, reload persistence, exact protected-document/tab checks and teardown. Its visual check exposed compressed paper and one-letter cell wrapping with both sidebars open; that failure remains recorded. PR #1122 includes a fixed-width canvas repair verified locally at 1280px, 910px and 390px, plus Gmail rich compose, safe remote-image display and inline-attachment fallback controls. It has 145 Gmail and 278 Docs/Workspace regressions, Chrome CUA checks and native-state captures. PR #1122 passed CI, merged at `61fb78b6`, and both production deployment workflows passed. Fresh hosted Chrome/native checks confirmed rich MIME, inline/binary attachment preservation, hidden remote images, fixed 816px Docs paper at 910px/390px, unchanged protected records and teardown. The same run found a stale Gmail post-send row race and empty-tab cursor insertion error; both are fixed in PR #1125 with 427 regressions plus delayed-response, desktop/mobile and native-state checks. CI and both production deployments passed at `95e755a1`. Fresh hosted checks passed empty-tab insertion, Gmail send/refresh/read and GitHub artifact download/mobile deletion with exact protected state and teardown. API and Chrome artifact downloads returned identical verified ZIP bytes; see `evidence/mailbox-cursor-artifacts-production-2026-09-24.json`. See `evidence/gmail-docs-production-2026-09-24.json` and `evidence/mailbox-load-and-docs-cursor.json`. See `evidence/docs-table-options-production-2026-09-24.json`, `evidence/docs-page-geometry.json` and `evidence/gmail-rich-compose.json`.

Gmail filter delivery from PR #1123 is integrated into PR #1127: label actions on message insert/import, full MIME body and attachment criteria, protected thread state, and permissive malformed-query evaluation. A disposable live Gmail comparison corrected two assumptions: inserts also apply filters, and invalid query terms are accepted but do not match, with surrounding negation preserved. The final implementation passes 188 Gmail regressions, 289 combined Gmail/Linear regressions and 772 contract checks. All nine live synthetic messages were confirmed trashed and all test filters/labels removed, with no pre-existing mail touched or mail sent. CI and hosted validation of the final head remain pending; forwarding is still open. See `evidence/gmail-filter-delivery.json` and `evidence/gmail-filter-provider-reference.json`.

Gmail compose windows and More options are in twin PR #1124: minimize/fullscreen, persisted default fullscreen, expandable Cc/Bcc, plain mode and an isolated draft print view. Local Chrome desktop/390px and native MIME checks pass with protected state unchanged. Native print-dialog/afterprint inspection remains blocked by the host's native UI availability; no print job was submitted. CI, deployment and hosted checks remain pending. See `evidence/gmail-compose-window.json`. These feature checks do not close the full eight-provider parity gate; no final ZIP has been created.

Docs rectangular table merge/unmerge passed CI and merged in PR #1126 at `6c739745`, with 295 Docs/Workspace, 345 Drive/Sheets and 14 focused merge tests. Live-provider fixtures cover native content, indices, styles, LTR/RTL, empty/mixed cells and partial unmerge; all five live test documents were confirmed trashed. Chrome desktop/390px and protected-tab checks pass. Both production deployment workflows passed. Fresh hosted verification is in progress; see `evidence/docs-merged-cells.json`.

Linear resource mentions and document content editing are in PR #1127, with an inline picker, native URL Markdown, keyboard selection, paragraph-preserving insertion and safe resource links. All 101 Linear tests and 289 combined Gmail/Linear tests pass. API person URL aliases and duplicate-title/renamed document targets now have regression coverage. Chrome desktop/390px comment/document save and reload checks pass. CI, deployment and hosted checks are pending; notification delivery, full collaboration and provider visual parity remain open. See `evidence/linear-mentions.json`.

A disposable live Gmail fixture confirmed that text inside an attached email is searchable and can trigger incoming filters. The twin retains that behavior and adds import/insert regressions; both test messages were trashed and the test filter/label removed. See `evidence/gmail-attached-email-provider-reference.json`.
