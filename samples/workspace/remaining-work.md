# Remaining work after the Sheets validation PR

PR #1170 merged as `4372d366` after full CI (6,033 twin/2,130 root tests) and independent OAuth review passed. Both production workflows, serving API/proxy images, 15 fresh native/HTTP check groups and all three worker images pass. Bounded hosted Chrome comment cancellation, teammate authorship, author controls and true reload pass; exact native state and teardown are confirmed. Preserved failed attempts include a discovered scope mismatch: a read/write-only token incorrectly rejects comment creation. Subsequent identity checks explicitly granted comments:create; the scope fix in #1172 now passes fresh hosted conformance. Mobile/touch, broader permissions and AI/collaboration remain open. See `linear-ui-principals-2026-09-25.json`.

PR #1171 matches the real Sheets warning text and adds accessible Close cancellation. Forty-seven focused tests, independent review and full CI (6,035 twin/2,130 root) pass; merged as `9e18dd55`. Both production workflows, serving API/proxy images, 17 fresh native/HTTP check groups and all five Workspace containers pass. Hosted desktop/390px Close, checked-suppression cancellation, warning recurrence, reload and keyboard Close pass with every captured Drive/Docs/Sheets native state unchanged; teardown confirmed. An initial local coordinate mis-target is preserved separately. Physical touch and exhaustive warning/protection parity remain open. See `sheets-warning-close-2026-09-26.json`.

PR #1172 adds recipient-scoped issue notifications and Inbox controls, transactional bulk operations, thread/edited-mention delivery and the read/write scope correction. All 271 Linear regressions and bounded local desktop/390px native-state checks pass; the bulk-failure and notification-history retention fixes have independent source/API confirmation. Full CI (6,083 twin/2,130 root tests) passes; merged as `db8d452d`. Both production workflows, serving API/proxy images and fresh hosted native/desktop/390px verification pass; all containers match their publications and teardown is confirmed. Project notifications, full settings, external delivery, realtime, permissions and AI remain open. See `linear-inbox-notifications-2026-09-26.json`.

PR #1169 adds the native protection sidebar and warning-only grid confirmation. Full CI (5,974 twin/2,130 root) passes; merged as `161af4ac`. Both production workflows, serving API/proxy image checks, 17 fresh native/HTTP check groups and all five Workspace-run containers pass. Fresh Chrome warning Cancel/OK/Escape/suppression/reload, relative formula copy and sidebar invalid-input/cancel/description persistence pass with exact native state comparisons and teardown confirmed. The private real Sheets reference was moved to recoverable trash. Mobile/touch, dialog copy/Close-control differences and broader protection parity remain open. See `sheets-protection-sidebar-2026-09-25.json`.

This is the handoff checklist after PR #1147. It is not a claim of eight-provider parity. The five tasks retain earlier passing hosted candidate episodes; later WKS-03 attempts include a business failure and a separately preserved browser-driver infrastructure timeout, while final WKS-04 exposed malformed Gmail send acceptance and now passes fresh #1150 revalidation; WKS-03 remains pending; the remaining frontend, final-deployment revalidation and package gates still prevent a final ZIP. Retain text/JSON evidence; do not restore screenshot files.

PR #1168 adds protected-range enforcement and inherited Workspace grants. All 134 focused/1,227 Workspace checks and full CI (5,928 twin/2,130 root) pass; merged as `625a11b3`. Both production workflows and serving API/proxy images pass. Fresh hosted direct/mapped/structural/dimension denials preserve all three native states; warning and explicit-editor writes, inherited access/revocation and unrelated fixture preservation pass. All five Workspace-run and ten fresh eight-provider container images match publication, with teardown confirmed. Hosted GUI and broader protection/provider parity remain open. See `sheets-protection-access-2026-09-25.json`.

## Release the reviewed changes

- Confirm both production workflows for the final merged twin/API revisions. Start fresh Twin Runs and verify the served implementation, native outcomes, protected state and teardown. Earlier ten-container and API/proxy attestations pass. The latest live-clock Gmail run exposes a supporting app image mismatch against its own builder publication. PR #1173 now isolates publication tags; deployment and all 13 containers across two concurrent fresh runs pass. Preserve the original mismatch and repeat full final-deployment attestation.
- Hosted relative-date/Scenario-clock checks for PR #1146 now pass. PR #1147 hosted native outcomes pass for rule lifecycle, range dropdown refresh, checkbox mouse/keyboard actions, warning/rejection, relative formulas and mobile cancellation/reload. PR #1149 passed CI and both production deployments (`64c8c14b`); fresh hosted desktop queued successful/failed-write restoration and native state pass. The fresh 390px queued-edit/reload replay now also passes with protected state unchanged and teardown confirmed; PR #1153 also passes full CI, both deployments and fresh hosted desktop/390px initial invalid/unavailable-marker checks before focus after load, reload, horizontal scrolling and sheet switches. Complete captured native state is unchanged and teardown is confirmed.
- Gmail native Print/afterprint now passes with two cancelled previews and unchanged native state. PR #1124 passed CI/review and merged as `94ab5965`; both production workflows passed. Fresh hosted native Print verification now also passes two preview/cancel/afterprint cycles with complete native state unchanged and teardown confirmed. No print job or PDF save was submitted.
- PR #1150 now completes malformed Gmail outbound MIME validation and native GitHub comment GET/PATCH/DELETE/list behavior. Full CI, both deployments, hosted conformance and affected WKS-04/WKS-05 candidate revalidation pass; all earlier outcomes remain preserved. Continue the wider frontend/permission audits.
- PR #1157 adds deployment-scoped container image IDs/registry digests and Cloud Run revision/traffic/image evidence. Local mocked and real-Docker checks and full CI pass; merged as `19518bc1`. Production Runtime and Workers rollouts pass. Fresh evidence covers all ten Compose containers and the two serving Cloud Run revisions, with every image matched to publication records, no missing containers and teardown confirmed. The observation is timestamped and does not inventory unrelated production workloads.
- PR #1163 fixes values-batch atomicity and body-read revocation (1022 local checks); full CI passes (5748 twin/2130 root) and it merged as `c04ee6aa`; both production workflows, four fresh hosted rejection paths, ordered updates/formulas and all five container images pass. Browser comparison remains pending. PR #1164 fixes effective Workspace grants and expiration (1092 local checks, including removeExpiration) and is now included in combined #1166, whose CI, production, fresh native/HTTP and five-container verification pass. These bounded native corrections do not close the broader frontend/permission gates.
- PR #1165 fixes Docs/Sheets initial file visibility and browser principal preservation; 20 focused checks and 1027 editor regressions pass. Delivered through #1166 with production/native/HTTP/image checks; Chrome catalog/editor/revocation verification now passes; complete reader-mode controls and access requests remain open.
- Reconcile and review the evidence PR #1145 and sample PR #14 against the final source revisions. Both remain drafts while full parity/release checks are open.

PR #1166 combines #1164 and #1165 with a fix for access revocation during request-body reads. All 91 focused and 1,138 broader combined checks and full CI (5,824 twin/2,130 root) pass. Merged as `5f67b252`; production, fresh hosted native/HTTP bootstrap, all five Workspace-run worker images and a subsequent fresh eight-provider ten-container attestation pass, with teardown confirmed. PRs #1164/#1165 are closed as incorporated. Browser GUI checks now pass within the scope recorded in workspace-principals-browser-2026-09-26.json; full reader/access-request parity remains open.

PR #1167 fixes access revoked during Notion saved-view and group form submission. All 15 reproduced failures and 187 Notion regressions pass. Full CI passes (5,839 twin/2,130 root); merged as `7854c3c7`. Both production workflows, fresh hosted form success/denial/stale-revision/native reload checks and all three container images pass; teardown is confirmed. See `notion-form-revocation-2026-09-25.json`.

Fresh hosted Chrome checks for #1166 now verify editor-only catalogs, authorized Docs/Sheets edits and reload, 390px layout, and immediate revocation. Denied writes preserve all native state; private fixtures remain intact; all five container images match publication and teardown is confirmed. The checks expose owner-labeled avatars and incorrect native lastModifyingUser attribution; their correction is incorporated in combined PR #1174 (original #1175), with 1,606 combined local regressions passing. Exact-head CI, deployment and fresh hosted identity/permission, desktop/390px and image checks now pass; teardown is confirmed. Denied-link HTML and full reader/access-request behavior remain open. See `workspace-principals-browser-2026-09-26.json`.

## Complete the eight frontend surfaces

| Twin | Work still required |
| --- | --- |
| GitHub | Complete advanced file/line review and review-management flows; remaining Projects field/view/grouping/automation controls; full Actions dispatch/log/run-management coverage; repository/organization settings, admin, analytics and AI/collaboration surfaces. Shared issue/PR conversation creation, editing, deletion, copy and quote controls now pass full CI, both deployments, and hosted desktop/390px native-state checks with protected state unchanged and teardown confirmed. Existing review lifecycle, requested-reviewer and artifact checks cover only selected flows. |
| Gmail | PR #1162 fixes historical-message date labels and explicit clock provenance; 252 local regressions and bounded desktop/390px reload/native-state checks pass, and full CI passes (5733 twin/2130 root); merged as `3f91765b`; both production workflows, API/proxy serving images and two fresh fixed/live-clock native/source/reload checks with all ten worker images pass. Native state is unchanged except Linear logical time and teardown is confirmed; fresh hosted fixed-clock Chrome desktop/390px labels/details/reload now pass with native resources unchanged and all ten images matched; live-clock Chrome also passes, but its supporting app image differs from its own builder publication; the #1173 deployment correction and fresh concurrent-run image checks now pass. Finish inline composer/remaining menu fidelity; complete settings/delegation, account/admin, AI and collaboration coverage. Verify all contextual actions across inbox, conversation, draft, search, labels and mobile states. |
| Google Calendar | PR #1159 adds the five native personal email notification preferences; 117 local tests, desktop/390px cancellation/retry/reload, delayed-save and protected-state checks pass. Full CI passes (5,513 twin / 2,130 root tests); merged as `7a314dac`. Both production workflows, fresh hosted desktop/390px preference checks, exact protected native-state comparisons and ten-container plus API/proxy image attestation pass; teardown confirmed. Finish access-request flows, side apps and reminder/notification delivery semantics; complete settings, sharing/admin, AI and collaborative flows. Verify cross-account permission changes and timezone/date behavior across the remaining views. |
| Google Docs | Default header/footer/footnote editing in #1155 now passes deployments, native checks, 20 export variants and bounded hosted desktop/390px GUI checks with protected state unchanged; complete remaining page/section variants, richer exports, suggesting and review interactions, richer history comparisons, Drive picker/image upload, table positioning/pinning and complete menus. Finish concurrent-edit behavior and full settings/AI coverage while preserving native document structure and permissions. |
| Google Sheets | Complete saved/temporary filter-view frontend and safe sorted-view editing (merged #1161 has private grid projection, saved/temporary browser controls, revision-checked queued typed writes, recovery and internal/external copy projection; 1007 local Sheets/Docs/Workspace regression tests and selected desktop/390px native-state checks pass; cut/hidden-row clipboard, mapped writes/protection, remaining provider dialog/permission fidelity remain pending; fresh hosted desktop/390px formula/copy, duplicate read recovery, temporary Save/Exit, failure/retry and reload now pass with exact protected-state checks and ten image matches; source CI passes 5723 twin/2130 root tests and both production workflows and API/proxy image observations pass), while hosted color verification now passes (fresh desktop/390px fill/text filtering, sorting, keyboard activation, failure/cancel/reload and exact native protected-state checks pass; latest 115 focused tests, full source CI with 5,478 twin/2,130 root tests and independent spill-gradient fix review pass; both production attempt 2 workflows and complete fresh ten-container plus serving API/proxy image attestation pass; both premature push-triggered attempts were cancelled before publication or rollout); charts/pivots and protected ranges; undo/redo; noncontiguous selection, header/frozen-boundary dragging and autoscroll. Finish validation chip colors/multiple selection and comprehensive clipboard/structural-reference behavior. Complete collaborative editing, settings/AI and remaining grid/menu controls. Initial invalid-marker rendering now passes bounded hosted checks. |
| Notion | Teamspaces, guests and full workspace/admin/permission flows; advanced database layouts and inline property editing, including relation/formula/rollup/files behavior. Finish AI and realtime collaboration while preserving the implemented page grants, groups and database filtering/sorting. |
| Linear | Native comment threads in #1156 now pass CI, deployments, native/form and bounded hosted desktop/390px GUI checks, including save failure recovery and protected state; complete remaining issue/project workflow and settings controls, inline collaboration, project graphs, uploads and notifications, realtime behavior and AI surfaces. Extend the existing rich editor, links and project overview with the remaining provider flows. |
| Stripe | Complete reporting/analytics, billing/account/admin and AI surfaces; remaining fees, FX/conversion and transaction workflows. Every exposed action must update native state and preserve the correct currency/amount semantics. Existing invoice/refund and currency-ledger checks do not establish full dashboard parity. |

## Perform the complete fidelity audit

- Expand the generated source-candidate index (`scripts/inventory_twin_frontends.py`, `evidence/frontend-source-inventory.json`) into a live route-by-route, state-by-state inventory for all eight twins: buttons, menus, fields, dialogs, navigation, keyboard shortcuts and permission-dependent controls. Source occurrences are not audited or unique rendered controls.
- For each control, compare the real provider and twin, exercise its intended result and error/cancellation behavior, reload, and verify the native state. No placeholder success messages or disconnected local-only state count as completed features.
- Compare desktop and narrow layouts, typography, spacing, scrolling, overlays and empty/loading/error states. Existing bounded 390px checks do not constitute exhaustive pixel parity or physical mobile/touch validation.
- Real Notion reference remains at a login screen. Linear is now authenticated: its Agent, AI settings, security controls and notification settings were inspected read-only, including menu cancellation. Full permission enforcement, native outcomes and collaboration comparisons remain pending. Stripe reporting and Assistant entry points were inspected read-only, but response and permission behavior remain unaudited.
- Verify shared API/browser state, multi-user permissions and revocation, concurrent changes, persistence, reset and run isolation across the remaining flows.

## Revalidate and package the sample

- Keep all five task prompts open to Computer Use, APIs or a mixture, with readily available GitHub/Gmail/Calendar/Sheets/Docs frontends and equivalent outcome grading.
- Preserve the five earlier passing hosted candidate reports and their original attempts/retry provenance, plus the correctly scored WKS-03 business failure, final WKS-04 unsafe artifact and same-state WKS-05 empty-index regrade. Preserve the separate WKS-03 browser-driver timeout as infrastructure invalid with its one completed exact-Scenario retry: business failure from unread approved Gmail terms, no unsafe side effects, teardown confirmed. Do not hide failures or retry that ordinary completed failure solely to obtain a pass. WKS-04 and WKS-05 pass fresh revalidation after #1150 with teardown confirmed; broaden browser-agent coverage where the prior successful candidate chose APIs.
- Run final regression and package-gate checks; refresh evidence hashes, source-tree hash, manifests and deployed revisions. Keep the eight frontend gates pending until their full audits actually pass.
- Only then permit the packager to create the operator-only ZIP, inspect its contents, and deliver the archive with run instructions. Candidate-facing handoffs must continue to exclude operator fixtures, grading contracts and credentials.

PR #1173 fixes the shared app publication tag exposed by the latest live-clock Gmail run. All 132 focused deployment/preview checks pass, and three regressions fail with the old tag allocator. Unique references are persisted per rendered bundle while build caches remain shared. Full CI (6,083 twin/2,134 root tests) passes; merged as `bd0c16ea`. Runtime and Workers rollouts and serving API/proxy images pass. All 13 containers across two concurrent fresh runs match their own publications, and both runs are torn down. The original failed image comparison is preserved. See `app-image-publications-2026-09-26.json`.

PR #1174 adds native issue subscriptions, Activity self/member controls, keyboard shortcuts and an actor-scoped Subscribed list. All 322 Linear regressions, bounded desktop/390px failure/retry/focus/search/reload checks and exact protected-state comparisons pass. The independently found IssuePayload typename mismatch is corrected and confirmed. Exact-head CI, deployment and fresh hosted native/desktop/390px/image verification pass with teardown confirmed; full permissions/settings/realtime/AI and exhaustive parity remain open. See `linear-issue-subscriptions-2026-09-26.json`.

Combined PR #1174 also incorporates Workspace identity #1175 and the Linear malformed-description/introspection review fixes. All 1,606 combined local regressions pass. Workspace native authorship, requester flags and local desktop/390px avatars are covered; full exact-head CI (6,143 twin / 2,134 root), production and fresh hosted file/revision/change-feed attribution, browser edits/revocation and all eight containers across two runs now pass. Both runs are torn down. See `workspace-editor-identity-2026-09-26.json`.

PR #1179 implements native Sheets Undo/Redo and repeat. All 1,316 Workspace regressions and bounded real-reference/local desktop/390px checks pass; eight independent source/native/reference review findings are resolved. Exact-head CI passed and #1179 merged as `b711e3a7`; both production workflows, fresh native and Chrome desktop/390px checks, all five runtime images, exact protected-state checks and teardown pass. It does not close comprehensive formula/clipboard, mobile/touch or sample gates. See `sheets-undo-redo-2026-09-26.json`.

PR #1180 corrects Linear Inbox popover accessibility and dark-label contrast. All 322 Linear regressions, bounded desktop/390px checks and 71 targeted snooze tests pass; all four preset accessible names retain their local timestamp. Independent review is clear within that scope. Exact-head CI passes (6,143 twin / 2,134 root); merged as `8d4355f2`. Both production workflows, serving API/proxy images, fresh native/desktop/390px checks and all three runtime containers pass; business state is preserved and teardown is confirmed. See `linear-inbox-accessibility-2026-09-26.json`.

The sample browser proxy now preserves Sheets page/group history headers and history warning-confirmation revisions while retaining credential and route isolation. All 195 sample/protocol regressions pass. Fresh hosted native/desktop/390px history and warning verification passes; no release or candidate gate changes. See `sample-sheets-history-proxy-2026-09-26.json`.

Fresh hosted #1161 browser checks now pass bounded sorted-formula/strict-rejection, internal copy, post-commit duplicate read recovery, copy rename, temporary-view Save/Exit, injected save-failure/retry and reload behavior. Six unrelated providers and protected Workspace data remain unchanged; Docs reflects only the shared spreadsheet metadata/change feed. All ten containers match publication and teardown is confirmed. Browser-tool shortcut/viewport attempts are preserved separately. Broader clipboard, permission, physical touch and exhaustive parity remain open. See `sheets-filter-view-projection-2026-09-25.json#/hosted_gui_2026_09_26`.

PR #1182 fixes GitHub OAuth collaborator identity in REST, GraphQL and browser reviews. All 384 GitHub and 107 compatibility regressions pass (2 compatibility skips); exact-head CI and fresh CLEAN merge checks pass. Merged as `4a564014`; both production workflows, serving API/proxy images, fresh native and Chrome desktop/390px attribution checks, all three container images and teardown pass. Exactly two intended collaborator reviews were added; all other full native state was preserved. Advanced pending-review privacy remains a confirmed defect, and the unconfigured seeded PR timestamp needs a clock correction. See `github-oauth-actors-2026-09-26.json`. Full release gates remain open.

PR #1183 adds native embedded chart rendering and editing for seven common chart types. All 1,332 integrated Sheets/Docs/Drive/Workspace regressions pass, including 16 chart cases. Bounded real-reference/local desktop/390px checks cover editing, native reload, move/resize/delete, error/retry/cancellation and selected-chart isolation; independent review scored four findings resolved and documentation was checked. Exact-head CI passed and #1183 merged as `5d95bdf7`; deployment and fresh hosted verification are pending. Additional chart families, publishing/export/chart sheets and full customization/touch parity remain open. See `sheets-charts-2026-09-26.json`.
