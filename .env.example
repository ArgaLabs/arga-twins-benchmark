ARGA_API_URL=https://api.argalabs.com
ARGA_CLI_BIN=arga
# Optional: the wrapper places this in an isolated temporary CLI config.
# Omit it when `arga login` has already populated ~/.config/arga/config.json.
ARGA_API_KEY=
